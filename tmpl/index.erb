<%
conn = PG.connect :dbname => "mail"
res = conn.exec('select * from alias order by id')
%>

<%= %>

<table>
<% res.each do |row| %>
    <tr>
    <td><%= row['id'] %></td>
    <td><%= row['address'] %></td>
<% end %>
</table>


